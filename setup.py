from setuptools import setup

setup(
    name="cyclic-pattern",
    version="0.0.1",
    install_requires=[],
    extras_require={
    },
    entry_points={
        "console_scripts": [
            "pattern = pattern:main",
        ]
    }
)
